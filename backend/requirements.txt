fastapi==0.110.3
uvicorn==0.30.1
sqlalchemy==2.0.31
pydantic>=2.8.0
pydantic-settings>=2.3.4
prometheus-client==0.20.0
pytest==8.2.2
httpx==0.27.0
email-validator>=2.0.0

# Database Drivers
# psycopg2-binary is installed automatically in the Docker container for production.
# For local development/testing on systems lacking C++ compilation tools (e.g. Windows Python 3.13),
# install pg8000 instead (uncomment below if running a local PostgreSQL database):
pg8000==1.31.2
